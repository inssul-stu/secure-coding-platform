name: secure_coding
channels:
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
    - flask 
    - flask-socketio
    - flask-sqlalchemy
    - flask-wtf  # CSRF 보호를 위해 추가
    - eventlet
    - werkzeug